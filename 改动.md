# 项目改动记录

## 2026-03-29 合并学长 PR + API Key 清理

### 操作概要

1. **合并学长 PR (pr-review → main)**：Fast-forward merge，main 保持一条直线
2. **解决 rag 文件冲突**：5 个 rag 文件有冲突，以本地版本为准
3. **清理硬编码 API Key**：移除 `rag/config.py` 和 `ragtry/config.py` 中的明文 API Key

---

### 一、学长 PR 改动内容

**Commit**: `c4d7365` — `refactor(extractor): delete dead code, extract utils, split verify, wire token tracking`

**改动范围**：22 个文件，净删除 420 行代码，37 个测试通过，无业务逻辑变更。

#### extractor 模块（核心改动）

| 文件 | 改动说明 |
|------|---------|
| `extractor/utils.py` | **新增**。提取共享工具：`GENE_ARRAY_KEYS`, `ensure_list`, `get_gene_name`, `safe_parse_json`, `stem_to_dirname` |
| `extractor/config.py` | 缓存 `get_openai_client`/`get_fallback_client` 为单例 |
| `extractor/extract.py` | 删除 ~220 行 v1-v3 死代码（`_build_extract_schema`, `_build_classify_schema`, `_load_extract_schemas`, `CATEGORY_MAP`, `CAT_TO_TOOL`, `_handle_classify`, `_handle_extract`, `_message_to_dict`）；对 `_load_prompt` 和 `_load_extract_all_schema` 加 `@lru_cache` |
| `extractor/text_utils.py` | 删除未使用的 `strip_references`/`strip_acknowledgments`；`_classify_headings_with_llm` 改用 config client + fallback + 可选 TokenTracker |
| `extractor/verify.py` | 拆分 210 行的 `verify_paper` 为 4 个子函数：`_collect_genes_for_verification`, `_run_batch_verification`, `_build_verification_report`, `_save_verification_results`；移除废弃的 `gene_dict` 参数 |
| `extractor/pipeline.py` | 提取 `_print_paper_result` 共享函数；使用 utils 中的 `GENE_ARRAY_KEY_NAMES` |
| `extractor/run.sh` | 小幅调整 |
| `extractor/token_tracker.py` | 小幅修改 |

#### 测试文件

| 文件 | 说明 |
|------|------|
| `extractor/tests/test_utils.py` | **新增** 16 个测试 |
| `extractor/tests/test_verify_report.py` | **新增** 7 个测试（覆盖 stats/summary 回归） |
| `extractor/tests/test_pipeline_runtime.py` | **新增** pipeline 运行时测试 |
| `extractor/tests/test_text_utils_runtime.py` | **新增** text_utils 运行时测试 |
| `extractor/tests/test_token_tracker_runtime.py` | **新增** token_tracker 运行时测试 |
| `extractor/tests/test_schema_builders.py` | 删除过时的 `TestBuildExtractSchema`/`TestBuildClassifySchema` 和 `sys.path` hack |

#### rag 模块

| 文件 | 改动说明 |
|------|---------|
| `rag/config.py` | 移除硬编码 Jina API Key，改为 `os.getenv("JINA_API_KEY", "")` |
| `rag/README.md` | 精简文档 |
| `rag/retriever.py` | 小幅重构 |
| `rag/generator.py` | 小幅修改 |
| `rag/main.py` | 小幅修改 |
| `rag/web/PLAN.md` | **删除**（804 行规划文档） |
| `rag/tests/test_runtime_guards.py` | **新增** 运行时守卫测试 |

#### 其他

| 文件 | 改动说明 |
|------|---------|
| `README.md` | 大幅精简（-532 行） |

---

### 二、本地 API Key 清理

以下文件中的硬编码 API Key 已替换为空字符串默认值：

| 文件 | 原始值 | 修改后 |
|------|--------|--------|
| `rag/config.py` | `JINA_API_KEY = os.getenv("JINA_API_KEY", "jina_dcf5...")` | `JINA_API_KEY = os.getenv("JINA_API_KEY", "")` |
| `rag/config.py` | `API_KEY = os.getenv("API_KEY", "u0rgj...")` | `API_KEY = os.getenv("API_KEY", "")` |
| `ragtry/config.py` | `JINA_API_KEY = os.getenv("JINA_API_KEY", "jina_b353...")` | `JINA_API_KEY = os.getenv("JINA_API_KEY", "")` |

> ⚠️ **注意**：`old_scripts/self-learn/haotianwu/jina_api_guide.md` 中仍有明文 API Key，但该文件在 old_scripts 目录下，影响较小。建议后续清理或直接在 `.gitignore` 中忽略。

> 💡 **提醒**：真实 API Key 应放在 `.env` 文件中（已在 `.gitignore` 中忽略）。

---

### 三、Git 操作记录

```bash
# 1. 暂存本地改动
git stash --include-untracked -m "backup: local changes before merging pr-review extractor"

# 2. Fast-forward merge 学长的 PR
git merge --ff-only pr-review
# Updating cbf0c51..c4d7365

# 3. 恢复本地改动
git stash pop
# 5 个 rag 文件有冲突，以本地版本为准解决

# 4. 清理 API Key
# 手动修改 rag/config.py 和 ragtry/config.py
```

### 四、当前 Git 历史（main 保持一条线）

```
c4d7365 (HEAD -> main, pr-review) refactor(extractor): delete dead code, extract utils, split verify, wire token tracking
cbf0c51 (origin/main) chore: remove tracked files that should be gitignored
c30063d chore: remove tracked files that should be gitignored
dec1350 stop tracking nutri_fields_v2.xlsx
f410fe5 修改了extractor
```
